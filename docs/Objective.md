This project is a FastAPI-based text summarization application using a fine-tuned Hugging Face T5 model. Users enter text through a web interface, the backend preprocesses the text, generates a summary using the model, and returns the result.

